spring:
  application:
    name: basketball-match-api
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/basketball}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
